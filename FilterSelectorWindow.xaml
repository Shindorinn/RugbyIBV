<Window x:Class="INFOIBV.FilterSelectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FilterSelectorWindow" Closing="Prevent_Closing"
        Height="262" Width="506">
    <Grid>
        <ListBox HorizontalAlignment="Left" ItemsSource="{Binding Path=InactiveFilters}" Margin="10,10,0,10" SelectedItem="{Binding Path=SelectedInactiveFilter}" Width="220"/>
        <Button Command="{Binding Path=AddToActiveFilterCommand}" Content="+" Height="100" HorizontalAlignment="Left" Margin="235,10,0,0" VerticalAlignment="Top" Width="20"/>
        <Button Command="{Binding Path=RemoveFromActiveFilterCommand}" Content="-" HorizontalAlignment="Left" Margin="235,0,0,10" Width="20" Height="100" VerticalAlignment="Bottom"/>
        <ListBox HorizontalAlignment="Right" ItemsSource="{Binding Path=ActiveFilters}" Margin="0,10,10,10" SelectedItem="{Binding Path=SelectedActiveFilter}" Width="220"/>
    </Grid>
</Window>
